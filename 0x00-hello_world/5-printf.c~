#include <stdio.h>
/**
 * main - prints to the standard out
 * printf - Display message to the screen 
 *
 * Return: return 0 to main function
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
