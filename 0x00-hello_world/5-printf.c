#include <stdio.h>
/**
 * main - prints to the standard out
 *
 * Return: return 0 to main function
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
