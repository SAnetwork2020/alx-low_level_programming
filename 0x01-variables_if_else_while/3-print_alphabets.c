#include <stdio.h>
/**
 * main- prints the alphabet in lowercase, and then in uppercase.*
 * Return: 0
*/
int main(void)
{
char al;
for (al = "a"; al <= "z"; al++)
int putchar(int al);
for (al = "A"; al <= "Z"; al++)
int putchar(int al);
putchar("\n");
return (0);
}
